using UnityEditor;

public class QuestAssetModificationProcessor : AssetModificationProcessor
{
    /*
    public static string[] OnWillSaveAssets(string[] Paths)
    {
        if (QuestTreeEditor.ActiveEditor && QuestTreeEditor.ActiveEditor.hasUnsavedChanges)
        {
            QuestTreeEditor.ActiveEditor.SaveChanges();
        }

        return Paths;
    }
    */
}
